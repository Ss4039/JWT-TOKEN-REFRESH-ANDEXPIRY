# JWT-TOKEN-REFRESH-ANDEXPIRY
JWT (JSON Web Token) is a compact, URL-safe way to represent claims (user data, expiry, permissions, etc.) between two parties — usually a client (browser/app) and a server (API).
